export default class PlayGame {
  constructor(data) {
    this.elem = data.elem;
    this.rabbit = data.rabbit;
    this.carrot = data.carrot;
    this.startPlay();
  }

  startPlay() {
    // todo : init!
  }

  stopPlay() {
    // todo : stop play
  }
}
